# Tyson Lemire's Personal Blog

Uses [jekyll](https://jekyllrb.com/docs/) to serve markdown as html pages.

## Serve site locally:
```zsh
bundle exec jekyll serve --livereload
``` 

Browse to: `http://localhost:4000`